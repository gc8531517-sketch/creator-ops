# 安全与隐私

- 不提交登录凭证、Cookie、appSecret、个人配置、原始Excel、评论、素材或聊天记录。
- 运行时数据仅保留在配置的 data_dir；只有明确使用飞书命令时向自己的飞书空间传输相应数据。采集与云同步本身不向大模型发送内容。
- 没有评论发送、私信、删除抖音作品或自动发布接口。
- Chrome 扩展仅需要 storage 和创作者中心页面访问；没有读取密码、Cookies 或上传用户数据的代码。显式任务 URL 可以触发导出，请不要打开来源不可信、带导出任务参数的链接。
- 仓库扫描不是完整的秘密检测器；公开前须同时审核允许文件清单、压缩包内容及 Git 历史。
- 提交安全报告时使用维护者提供的私密渠道（如 GitHub private vulnerability reporting）；未启用时先用无敏感细节的 Issue 请求渠道，不能公开漏洞利用凭证。

限制：单个安装实例使用 OS 锁，不能协调另一台电脑或另一个安装副本。请不要对同一个 Chrome 下载目录和 Base 开启多个独立写入者。
